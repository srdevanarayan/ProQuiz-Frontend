import React from "react";

const addQuestion = ({ selectedQuestion, refreshPage, cat }) => {
  return (
    <>
      <div className="contributoraddquestion">
        <button>Hello</button>
      </div>
    </>
  );
};

export default addQuestion;
